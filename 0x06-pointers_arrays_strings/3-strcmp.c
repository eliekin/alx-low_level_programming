#include "main.h"
#include <string.h>
/**
* _strcmp - compares two strings
* @s1: parameter
* @s2: parameter
* Return: returns integer
*/
int _strcmp(char *s1, char *s2)
{
	int i;

	i = strcmp(s1, s2);
	return (i);
}
