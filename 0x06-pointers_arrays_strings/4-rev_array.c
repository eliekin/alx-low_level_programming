#include "main.h"

/**
* reverse_array - reverses an array
* @a: parameter
* @n: parameter
* Return: void
*/

void reverse_array(int *a, int n)
{
	int i, j;

	for (j = n - 1; j >= 0; j--)
	{
		a[j];
	}
}
