#include <string.h>
#include "main.h"
/**
* _strlen - prints the length
* @n: parameter
*
* Return: integer
*/

int _strlen(char *s)
{
	int lengthValue;

	lengthValue = strlen(s);
	return (lengthValue);
}
